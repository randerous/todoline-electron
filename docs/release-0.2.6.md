# 0.2.6

- 修复标签进入重命名状态时被固定宽度输入框撑宽的问题。
- 重命名框沿用原标签名称区域的宽度，进入编辑、输入更长名称和取消时均保持宽度稳定；确认后按新名称正常排布。
- 调整输入框内边距，完整显示新建文档默认的日期名称，保持无额外蓝色边框。

## 验证与文件

类型检查、生产构建、打包后的 3 项重命名回归及 3 种名称的宽度检查全部通过，并确认日期名称完整显示、无额外焦点描边。仅检查本次修改及重命名相关回归。打包程序验证结果记录于 `packaged-0.2.6.json`，归档一致性与 SHA-256 记录于 `release-0.2.6.json`。

程序：`dist/TodoLine-Electron-0.2.6-win-x64.zip`

源码：`dist/TodoLine-Electron-0.2.6-source.zip`

解压版：`dist/release-0.2.6/win-unpacked/TodoLine Electron.exe`

保留旧版本，仅本地打包。
