# 0.1.47

- 正文右键菜单的“继续编号 / 重新编号”与其他菜单项共用圆角、字号、高度及内边距，仅缩短横向宽度。
- “置顶事件”的箭头与快捷键列右对齐，统一使用快捷键的辅助文字颜色。
- EXE、任务栏和托盘图标原样恢复 0.1.45 方案：蓝绿至靛蓝渐变，三条线长度比例 18∶12∶14。窗口内标识保持不变。

按要求仅验证修改相关部分：直接启动最终 EXE，检查两个编号按钮的实际悬停样式、重新编号和继续编号操作、置顶箭头的位置和颜色；核对图标生成器与 PNG、ICO 均与 0.1.45 源码包逐字节一致，并核对最终 EXE 内嵌图标。没有运行全量测试。

实测按钮高度 28px、圆角 4px、内边距 5px 10px；箭头与快捷键右边缘一致，颜色一致。生产构建及发布包 CRC、ASAR 编译文件一致性校验通过。

便携包：`dist/TodoLine-Electron-0.1.47-win-x64.zip`。

本机 EXE：`dist/release-0.1.47/win-unpacked/TodoLine Electron.exe`。
