# 0.2.7

- 文件比较标题由 Diff 改为 **Compare**。
- 类型说明替换为“从左侧修改到右侧，需：”，明确新增、删除、修改行数的方向。
- 标题栏文字统一为 13px，仅 Compare 标题加粗。
- 移除“差异较大的部分按整段对齐显示。”提示行，为正文比较留出空间；对齐算法保持不变。
- 工具栏按钮说明统一为“与前一个标签比较”。

## 验证与文件

类型检查、生产构建和打包后 5 项文件比较回归全部通过。另验证标题栏文案、字号、字重及 1,500 行差异时不出现整段对齐提示，并检查深浅主题效果。仅检查本次修改及文件比较相关回归。打包程序验证结果记录于 `packaged-0.2.7.json`，归档一致性与 SHA-256 记录于 `release-0.2.7.json`。

程序：`dist/TodoLine-Electron-0.2.7-win-x64.zip`

源码：`dist/TodoLine-Electron-0.2.7-source.zip`

解压版：`dist/release-0.2.7/win-unpacked/TodoLine Electron.exe`

保留旧版本，仅本地打包。
