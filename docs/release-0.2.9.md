# 0.2.9

- 格式转换成功后不再显示提示条；原文件的恢复副本仍保留，实际失败仍提示。
- Markdown 正文顶部留白由两层共 49px 缩减至 14px；目录顶部留白减少 14px。
- “插入公式”使用与其他工具栏按钮一致的 16px 矢量图标，统一间距并垂直居中。
- 正文／标题选择器取消蓝色焦点边框，下拉菜单统一使用主题背景、文字颜色、圆角边框、阴影和选中样式，保留鼠标和键盘操作。

## 验证

类型检查、生产构建、4 项打包后界面回归及专项界面检查通过；覆盖转换提示与恢复副本、目录导航、标题快捷键、公式插入、三种主题下拉样式和顶部间距。110 个构建文件与程序归档逐一匹配。

## 文件

程序：`dist/TodoLine-Electron-0.2.9-win-x64.zip`

源码：`dist/TodoLine-Electron-0.2.9-source.zip`

解压版：`dist/release-0.2.9/win-unpacked/TodoLine Electron.exe`

保留旧版本，仅本地打包。验证记录见 `packaged-0.2.9.json`，程序归档一致性与 SHA-256 见 `release-0.2.9.json`。
